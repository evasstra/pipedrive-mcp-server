// This file provides a basic type declaration for the 'pipedrive' module,
// which does not ship its own types. This tells TypeScript to treat the
// module as type `any`, which allows us to use it without full type safety.
declare module 'pipedrive';
